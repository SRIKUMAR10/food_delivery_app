import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Golden Tests for Store Details Page', () {
    testWidgets('Store details match golden image', (
      WidgetTester tester,
    ) async {
      // await tester.pumpWidget(MaterialApp(home: SellerStoreDetailsPage()));
      // await expectLater(find.byType(SellerStoreDetailsPage), matchesGoldenFile('store_details.png'));
    });
  });
}
