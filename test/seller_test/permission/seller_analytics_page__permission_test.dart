import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Permission tests', () {
    test(
      'verify analytics page does not require special permissions to view',
      () {
        expect(true, isTrue);
      },
    );
  });
}
