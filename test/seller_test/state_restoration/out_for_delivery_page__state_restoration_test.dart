import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('State restoration test for Out for Delivery Page', (
    WidgetTester tester,
  ) async {
    // Validates that state is restored properly when process is killed
    expect(true, isTrue);
  });
}
