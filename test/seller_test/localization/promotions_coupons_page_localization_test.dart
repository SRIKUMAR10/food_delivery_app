import 'package:flutter_test/flutter_test.dart';

void main() {
  group('PromotionsCouponsPage Localization Test', () {
    testWidgets('Strings are correctly localized', (WidgetTester tester) async {
      // In a fully localized app, this checks context.l10n values
      expect(true, isTrue);
    });
  });
}
