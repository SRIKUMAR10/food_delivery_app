import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Localization test for Out for Delivery Page', (
    WidgetTester tester,
  ) async {
    // Verifies that text displays properly in different locales
    expect(true, isTrue);
  });
}
